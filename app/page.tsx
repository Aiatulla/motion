import Rotate from "@/components/rotatedBox";

const Page = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <Rotate />
    </div>
  );
};

export default Page;
